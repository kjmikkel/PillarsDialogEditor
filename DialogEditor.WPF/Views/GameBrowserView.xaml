<UserControl x:Class="DialogEditor.WPF.Views.GameBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DialogEditor.ViewModels;assembly=DialogEditor.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252525" Padding="4,4,4,4" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Grid>
                <TextBox x:Name="FilterBox"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Background="#333" Foreground="#ccc" CaretBrush="#ccc"
                         BorderBrush="#555" BorderThickness="1"
                         Padding="6,3,24,3" FontSize="10"
                         KeyDown="FilterBox_KeyDown"
                         ToolTip="{StaticResource ToolTip_FilterConversations}"/>
                <!-- Placeholder -->
                <TextBlock Text="{StaticResource Placeholder_FilterConversations}" Foreground="#555" FontSize="10"
                           Padding="8,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FilterText}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <!-- Clear button -->
                <Button Command="{Binding ClearFilterCommand}"
                        HorizontalAlignment="Right" Width="20"
                        Content="&#x2715;" VerticalAlignment="Center"
                        Style="{StaticResource FlatIconButton}"
                        ToolTip="{StaticResource ToolTip_ClearFilter}"/>
            </Grid>
        </Border>

        <TreeView Grid.Row="1" Background="#2d2d2d"
                  BorderThickness="0"
                  ItemsSource="{Binding FilteredFolders}"
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">
            <TreeView.Resources>
                <!-- Keep selection colour identical whether the tree has focus or not -->
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                 Color="#1a5276"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                 Color="#1a5276"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                                 Color="White"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                                 Color="White"/>

                <HierarchicalDataTemplate DataType="{x:Type vm:ConversationFolderViewModel}"
                                          ItemsSource="{Binding Items}">
                    <TextBlock Text="{Binding DisplayName}"
                               Foreground="#888" FontSize="10" Padding="2,2"/>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type vm:ConversationItemViewModel}">
                    <TextBlock Text="{Binding Name}"
                               Foreground="#ccc" FontSize="10" Padding="2,2"/>
                </DataTemplate>
            </TreeView.Resources>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#ccc"/>
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
                    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

        <Border Grid.Row="2" Background="#1a1a1a" Padding="8,5">
            <TextBlock Text="{Binding GameName}"
                       Foreground="#555" FontSize="9"/>
        </Border>
    </Grid>
</UserControl>
