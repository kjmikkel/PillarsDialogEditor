<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:DialogEditor.ViewModels.Models;assembly=DialogEditor.ViewModels"
             xmlns:vm="clr-namespace:DialogEditor.ViewModels;assembly=DialogEditor.ViewModels"
             xmlns:types="clr-namespace:DialogEditor.ViewModels.Services;assembly=DialogEditor.ViewModels"
             x:Class="DialogEditor.Avalonia.Views.NodeDetailView"
             x:CompileBindings="False">

    <UserControl.Styles>
        <Style Selector="TextBlock.group-header">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Muted}"/>
            <Setter Property="FontSize"   Value="{StaticResource FontSize.Micro}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin"     Value="0,10,0,3"/>
        </Style>
        <Style Selector="TextBlock.field-label">
            <Setter Property="Foreground"  Value="{DynamicResource Brush.Text.Caption}"/>
            <Setter Property="FontSize"    Value="{StaticResource FontSize.Caption}"/>
            <Setter Property="Margin"      Value="0,4,0,2"/>
        </Style>
        <Style Selector="TextBox.detail-field">
            <Setter Property="Background"      Value="{DynamicResource Brush.Surface.Card}"/>
            <Setter Property="Foreground"      Value="{DynamicResource Brush.Text.Primary}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource Brush.Border.Strong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize"        Value="{StaticResource FontSize.Small}"/>
            <Setter Property="Padding"         Value="4,3"/>
            <Setter Property="Margin"          Value="0,0,0,4"/>
        </Style>
        <Style Selector="ComboBox.detail-combo">
            <Setter Property="Background"          Value="{DynamicResource Brush.Surface.Card}"/>
            <Setter Property="Foreground"          Value="{DynamicResource Brush.Text.Primary}"/>
            <Setter Property="BorderBrush"         Value="{DynamicResource Brush.Border.Strong}"/>
            <Setter Property="FontSize"            Value="{StaticResource FontSize.Small}"/>
            <Setter Property="Padding"             Value="4,2"/>
            <Setter Property="Margin"              Value="0,0,0,4"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
        <Style Selector="CheckBox.detail-check">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}"/>
            <Setter Property="FontSize"   Value="{StaticResource FontSize.Small}"/>
            <Setter Property="Margin"     Value="0,0,0,4"/>
        </Style>
    </UserControl.Styles>

    <UserControl.Resources>
        <DataTemplate x:Key="ReadOnlyRowTemplate" DataType="models:PropertyRow">
            <Grid ColumnDefinitions="110,*" Margin="0,1,0,1">
                <TextBlock Grid.Column="0" Text="{Binding Label}"
                           Foreground="{DynamicResource Brush.Text.Caption}" FontSize="{StaticResource FontSize.Small}" VerticalAlignment="Top"/>
                <TextBlock Grid.Column="1" Text="{Binding Value}"
                           Foreground="{Binding Style, Converter={StaticResource PropertyValueStyleToBrush}}"
                           FontSize="{StaticResource FontSize.Small}" TextWrapping="Wrap" LineHeight="15"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <TextBlock Text="{StaticResource Label_SelectNode}"
                   Foreground="{DynamicResource Brush.Text.Disabled}" FontSize="{StaticResource FontSize.Label}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   IsVisible="{Binding HasContent, Converter={StaticResource InverseBoolToVis}}"/>

        <ScrollViewer Background="{DynamicResource Brush.Surface.Subtle}" IsVisible="{Binding HasContent}">
            <StackPanel Margin="8">

                <!-- ── Attribution (git: who last edited this node) ────── -->
                <StackPanel IsVisible="{Binding HasAttribution}">
                    <TextBlock Classes="group-header" Text="{StaticResource NodeDetail_LastEditedLabel}"/>
                    <TextBlock Text="{Binding LastEditedSummary}"
                               ToolTip.Tip="{Binding LastEditedTooltip}"
                               Foreground="{DynamicResource Brush.Node.Quotidian.Note}" FontSize="{StaticResource FontSize.Small}" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- ── Default / Male text ─────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_DefaultMaleText}"/>
                <TextBox x:Name="DefaultTextBox" Classes="detail-field"
                         Text="{Binding DefaultText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="52"
                         ToolTip.Tip="{StaticResource ToolTip_DefaultText}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_DefaultText}"
                         AutomationProperties.Name="{StaticResource Label_DefaultMaleText}"/>

                <!-- ── Female text ─────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_FemaleText}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding FemaleText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="36"
                         Watermark="{StaticResource Placeholder_FemaleText}"
                         ToolTip.Tip="{StaticResource ToolTip_FemaleText}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_FemaleText}"
                         AutomationProperties.Name="{StaticResource Label_FemaleText}"/>

                <!-- ── Identity ────────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupIdentity}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_Type}"/>
                <ComboBox Classes="detail-combo"
                          ItemsSource="{x:Static vm:NodeDetailViewModel.NodeTypeOptions}"
                          SelectedItem="{Binding NodeTypeString, Mode=TwoWay}"
                          ToolTip.Tip="{StaticResource ToolTip_NodeType}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_NodeType}"
                          AutomationProperties.Name="{StaticResource PropertyRow_Type}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_SpeakerCategory}"/>
                <ComboBox Classes="detail-combo"
                          ItemsSource="{x:Static vm:NodeDetailViewModel.SpeakerCategoryOptions}"
                          SelectedItem="{Binding SpeakerCategoryString, Mode=TwoWay}"
                          ToolTip.Tip="{StaticResource ToolTip_SpeakerCategory}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_SpeakerCategory}"
                          AutomationProperties.Name="{StaticResource PropertyRow_SpeakerCategory}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_SpeakerGuid}"/>
                <!-- Name picker — visible when speaker data is loaded (PoE2) -->
                <AutoCompleteBox Classes="detail-field"
                                 ItemsSource="{Binding AvailableSpeakers}"
                                 SelectedItem="{Binding SelectedSpeakerEntry, Mode=TwoWay}"
                                 Watermark="{StaticResource Placeholder_SpeakerSearch}"
                                 FilterMode="Contains"
                                 IsVisible="{Binding HasSpeakerData}"
                                 ToolTip.Tip="{StaticResource ToolTip_SpeakerPicker}"
                                 AutomationProperties.HelpText="{StaticResource ToolTip_SpeakerPicker}"
                                 AutomationProperties.Name="{StaticResource PropertyRow_SpeakerGuid}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding SpeakerGuid, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         FontFamily="Consolas,Courier New,monospace"
                         ToolTip.Tip="{StaticResource ToolTip_SpeakerGuid}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_SpeakerGuid}"
                         AutomationProperties.Name="{StaticResource PropertyRow_SpeakerGuid}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_ListenerGuid}"/>
                <!-- Name picker — visible when speaker data is loaded (PoE2) -->
                <AutoCompleteBox Classes="detail-field"
                                 ItemsSource="{Binding AvailableSpeakers}"
                                 SelectedItem="{Binding SelectedListenerEntry, Mode=TwoWay}"
                                 Watermark="{StaticResource Placeholder_SpeakerSearch}"
                                 FilterMode="Contains"
                                 IsVisible="{Binding HasSpeakerData}"
                                 ToolTip.Tip="{StaticResource ToolTip_SpeakerPicker}"
                                 AutomationProperties.HelpText="{StaticResource ToolTip_SpeakerPicker}"
                                 AutomationProperties.Name="{StaticResource PropertyRow_ListenerGuid}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding ListenerGuid, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         FontFamily="Consolas,Courier New,monospace"
                         ToolTip.Tip="{StaticResource ToolTip_ListenerGuid}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_ListenerGuid}"
                         AutomationProperties.Name="{StaticResource PropertyRow_ListenerGuid}"/>

                <!-- ── Display ─────────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupDisplay}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_DisplayType}"/>
                <ComboBox Classes="detail-combo"
                          ItemsSource="{x:Static vm:NodeDetailViewModel.DisplayTypeOptions}"
                          SelectedItem="{Binding DisplayType, Mode=TwoWay}"
                          ToolTip.Tip="{StaticResource ToolTip_DisplayType}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_DisplayType}"
                          AutomationProperties.Name="{StaticResource PropertyRow_DisplayType}"/>

                <Border Background="{DynamicResource Brush.Bark.Detail.Background}" BorderBrush="{DynamicResource Brush.Bark.Detail.Border}" BorderThickness="1"
                        CornerRadius="2" Padding="6,4" Margin="0,0,0,4"
                        IsVisible="{Binding BarkWarnings.Count, Converter={StaticResource CountToVis}}"
                        ToolTip.Tip="{StaticResource ToolTip_BarkWarnings}">
                    <ItemsControl ItemsSource="{Binding BarkWarnings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="{DynamicResource Brush.Bark.Detail.Text}"
                                           FontSize="{StaticResource FontSize.Small}" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_Persistence}"/>
                <ComboBox Classes="detail-combo"
                          ItemsSource="{x:Static vm:NodeDetailViewModel.PersistenceOptions}"
                          SelectedItem="{Binding Persistence, Mode=TwoWay}"
                          ToolTip.Tip="{StaticResource ToolTip_Persistence}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_Persistence}"
                          AutomationProperties.Name="{StaticResource PropertyRow_Persistence}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_ActorDirection}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding ActorDirection, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         ToolTip.Tip="{StaticResource ToolTip_ActorDirection}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_ActorDirection}"
                         AutomationProperties.Name="{StaticResource PropertyRow_ActorDirection}"/>

                <!-- ── Voice ───────────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupVoice}"/>

                <TextBlock Classes="field-label" Text="{StaticResource PropertyRow_ExternalVO}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding ExternalVO, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         FontFamily="Consolas,Courier New,monospace"
                         ToolTip.Tip="{StaticResource ToolTip_ExternalVO}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_ExternalVO}"
                         AutomationProperties.Name="{StaticResource PropertyRow_ExternalVO}"/>

                <CheckBox Classes="detail-check"
                          IsChecked="{Binding HasVO, Mode=TwoWay}"
                          Content="{StaticResource PropertyRow_HasVO}"
                          ToolTip.Tip="{StaticResource ToolTip_HasVO}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_HasVO}"/>

                <CheckBox Classes="detail-check"
                          IsChecked="{Binding HideSpeaker, Mode=TwoWay}"
                          Content="{StaticResource PropertyRow_HideSpeaker}"
                          ToolTip.Tip="{StaticResource ToolTip_HideSpeaker}"
                          AutomationProperties.HelpText="{StaticResource ToolTip_HideSpeaker}"/>

                <!-- ── Comments ────────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource PropertyRow_Comments}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding Comments, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="30"
                         ToolTip.Tip="{StaticResource ToolTip_Comments}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_Comments}"
                         AutomationProperties.Name="{StaticResource PropertyRow_Comments}"/>

                <!-- ── Translator note ────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource NodeDetail_TranslatorNote}"/>
                <TextBox Classes="detail-field"
                         Text="{Binding TranslatorNote, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="30"
                         ToolTip.Tip="{StaticResource ToolTip_NodeDetail_TranslatorNote}"
                         AutomationProperties.HelpText="{StaticResource ToolTip_NodeDetail_TranslatorNote}"
                         AutomationProperties.Name="{StaticResource NodeDetail_TranslatorNote}"/>

                <!-- ── Scripts (edit in separate window) ─────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupLogic}"/>
                <Grid ColumnDefinitions="*,Auto" Margin="0,2,0,0">
                    <TextBlock Grid.Column="0"
                               Text="{Binding ScriptSummary}"
                               Foreground="{DynamicResource Brush.Text.Muted}" FontSize="{StaticResource FontSize.Small}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="1"
                            Content="{StaticResource Button_EditScripts}"
                            Click="EditScripts_Click"
                            Background="{DynamicResource Brush.Surface.Header}" Foreground="{DynamicResource Brush.Text.Muted.Light}" BorderThickness="0"
                            Padding="8,3" FontSize="{StaticResource FontSize.Small}"
                            ToolTip.Tip="{StaticResource ToolTip_EditScripts}"
                            AutomationProperties.HelpText="{StaticResource ToolTip_EditScripts}"/>
                </Grid>

                <!-- ── Read-only groups (node ID) ─────────────────────── -->
                <ItemsControl ItemsSource="{Binding PropertyGroups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Classes="group-header" Text="{Binding Name}"/>
                                <ItemsControl ItemsSource="{Binding Rows}"
                                              ItemTemplate="{StaticResource ReadOnlyRowTemplate}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- ── Conditions (edit in separate window) ───────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupConditions}"/>
                <Grid ColumnDefinitions="*,Auto" Margin="0,2,0,0">
                    <!-- Summary: show count + first condition names -->
                    <TextBlock Grid.Column="0"
                               Text="{Binding ConditionSummary}"
                               Foreground="{DynamicResource Brush.Text.Muted}" FontSize="{StaticResource FontSize.Small}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="1"
                            x:Name="EditConditionsButton"
                            Content="{StaticResource Button_EditConditions}"
                            Click="EditConditions_Click"
                            Background="{DynamicResource Brush.Surface.Header}" Foreground="{DynamicResource Brush.Text.Muted.Light}" BorderThickness="0"
                            Padding="8,3" FontSize="{StaticResource FontSize.Small}"
                            ToolTip.Tip="{StaticResource ToolTip_EditConditions}"
                            AutomationProperties.HelpText="{StaticResource ToolTip_EditConditions}"/>
                </Grid>

                <!-- ── Links ──────────────────────────────────────────── -->
                <TextBlock Classes="group-header" Text="{StaticResource Label_GroupLinks}"/>
                <ItemsControl ItemsSource="{Binding Links}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="vm:ConnectionViewModel">
                            <Grid RowDefinitions="Auto,Auto" Margin="0,2,0,2">
                                <!-- Row 0: target + delete -->
                                <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto,Auto">
                                    <TextBlock Grid.Column="0"
                                               Text="{StaticResource Link_Arrow}"
                                               Foreground="{DynamicResource Brush.Text.Info}" FontSize="{StaticResource FontSize.Small}"
                                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Target.Owner.NodeId}"
                                               Foreground="{DynamicResource Brush.Text.Info}" FontSize="{StaticResource FontSize.Small}"
                                               VerticalAlignment="Center"/>
                                    <!-- Condition editor button — dim when no conditions, accent when present -->
                                    <Button Grid.Column="2"
                                            Content="{Binding ConditionCount,
                                                StringFormat='⚙{0}'}"
                                            Click="LinkConditions_Click"
                                            Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            FontSize="{StaticResource FontSize.Small}" Padding="3,1" Margin="0,0,2,0"
                                            Foreground="{Binding HasConditions,
                                                Converter={StaticResource BoolToFemaleTextBrush}}"
                                            ToolTip.Tip="{StaticResource ToolTip_LinkConditions_None}"
                                            AutomationProperties.HelpText="{StaticResource ToolTip_LinkConditions_None}"/>
                                    <Button Grid.Column="3"
                                            Content="{StaticResource Button_DeleteLink}"
                                            Command="{Binding DataContext.DeleteLinkCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource Brush.Text.Muted}" FontSize="{StaticResource FontSize.Caption}" Padding="4,1"
                                            ToolTip.Tip="{StaticResource ToolTip_DeleteLink}"
                                            AutomationProperties.HelpText="{StaticResource ToolTip_DeleteLink}"/>
                                </Grid>
                                <!-- Row 1: editable properties -->
                                <Grid Grid.Row="1" ColumnDefinitions="*,Auto,Auto" Margin="12,2,0,0">
                                    <ComboBox Grid.Column="0"
                                              SelectedItem="{Binding QuestionNodeTextDisplay, Mode=TwoWay}"
                                              ItemsSource="{x:Static vm:ConnectionViewModel.QTDOptions}"
                                              Background="{DynamicResource Brush.Surface.Card}" Foreground="{DynamicResource Brush.Text.Secondary}"
                                              BorderBrush="{DynamicResource Brush.Border.Strong}" BorderThickness="1"
                                              FontSize="{StaticResource FontSize.Small}" Padding="3,1" Height="22"
                                              ToolTip.Tip="{StaticResource ToolTip_LinkQTD}"
                                              AutomationProperties.HelpText="{StaticResource ToolTip_LinkQTD}"
                                              AutomationProperties.Name="{StaticResource AutomationName_LinkQTD}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Converter={StaticResource QTDDisplay},
                                                                  ConverterParameter={StaticResource Option_QTD_Default}}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    <TextBlock Grid.Column="1"
                                               Text="{StaticResource Label_LinkWeight}"
                                               Foreground="{DynamicResource Brush.Text.Disabled}" FontSize="{StaticResource FontSize.Small}"
                                               VerticalAlignment="Center" Margin="6,0,4,0"/>
                                    <NumericUpDown Grid.Column="2"
                                                   Value="{Binding RandomWeight, Mode=TwoWay, Converter={StaticResource FloatDecimal}}"
                                                   Minimum="0" Maximum="100"
                                                   Increment="0.1" FormatString="0.##"
                                                   Background="{DynamicResource Brush.Surface.Card}" Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   BorderBrush="{DynamicResource Brush.Border.Strong}" BorderThickness="1"
                                                   FontSize="{StaticResource FontSize.Small}" Padding="2,0" Width="72"
                                                   ShowButtonSpinner="False"
                                                   ClipValueToMinMax="True"
                                                   ToolTip.Tip="{StaticResource ToolTip_LinkWeight}"
                                                   AutomationProperties.HelpText="{StaticResource ToolTip_LinkWeight}"
                                                   AutomationProperties.Name="{StaticResource Label_LinkWeight}"/>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Add link row -->
                <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,0">
                    <TextBox Grid.Column="0"
                             Text="{Binding AddLinkTargetId, Mode=TwoWay}"
                             Watermark="{StaticResource Placeholder_AddLinkTargetId}"
                             Background="{DynamicResource Brush.Surface.Card}" Foreground="{DynamicResource Brush.Text.Primary}"
                             BorderBrush="{DynamicResource Brush.Border.Strong}" BorderThickness="1"
                             FontSize="{StaticResource FontSize.Small}" Padding="4,3"
                             ToolTip.Tip="{StaticResource ToolTip_AddLinkTargetId}"
                             AutomationProperties.HelpText="{StaticResource ToolTip_AddLinkTargetId}"
                             AutomationProperties.Name="{StaticResource Placeholder_AddLinkTargetId}"/>
                    <Button Grid.Column="1"
                            Content="{StaticResource Button_AddLink}"
                            Command="{Binding AddLinkCommand}"
                            Background="{DynamicResource Brush.Surface.Header}" Foreground="{DynamicResource Brush.Text.Muted.Light}" BorderThickness="0"
                            Padding="6,2" Margin="4,0,0,0"
                            ToolTip.Tip="{StaticResource ToolTip_AddLink}"
                            AutomationProperties.HelpText="{StaticResource ToolTip_AddLink}"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
